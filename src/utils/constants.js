export const API_URL = 'https://jsonplaceholder.typicode.com/todos';

export const TODOS = {
    GET: `${API_URL}?userId=1&limit=10`,
    DELETE: `${API_URL}`,
    ADD: `${API_URL}`,
    PUT: `${API_URL}`
}

export const ENDPOINTS = {
    TODOS
}